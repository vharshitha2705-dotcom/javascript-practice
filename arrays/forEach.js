// -------------------------------------
// forEach() - Executes a Function for Each Element
// -------------------------------------

const fruits = ["Apple", "Banana", "Mango"];

fruits.forEach((fruit) => {
    console.log(fruit);
});