// -------------------------------------
// every() - Checks if All Elements Match
// -------------------------------------

const marks = [85, 70, 90, 75,25];

const allPassed = marks.every(mark => mark >= 35);

console.log(allPassed);